# Windows sub-18 startup investigation

Objective: improve ordinary Recommended startup toward repeated interactive menus below 18 s.
Owner: current Codex task. Finish condition: measure a validated candidate, retain only justified
changes, merge main and restore the ordinary Windows task; report the full observed range.

Baseline main `f4568354174d7be860df5b6dcfe8f57279e7e0f5`; installed executable source
`73d085d3fe5c5630a2d805f85a0de313860ec53a`, JAR SHA-256
`369d43b415829c5082c29d1762adb9d8fdb73f6cf73d6406087740bd38534cb9`.

Diagnostic `20260906-025847`, native graphics, 1024x720, phase and prepared-load attribution:
menu 20.479 s (diagnostic only). SpecStore 3.569 s; texture resources 7.928 s; final ScriptStore
join 63 ms; mod callbacks 3.610 s. Further compiler optimization is no longer the main target.
Prepared-buffer construction is 699 ms and main claim reads 506 ms. VM remains 20 GiB, with about
8.4 GiB host memory available; no additional RAM is justified.

Installed Windows TextureLoader still formats two debug upload-progress call sites unconditionally,
and formats/logs successful buffer cleanup at INFO. The diagnostic log contains 15,458 successful
cleanup messages, zero cleanup warnings, and no DEBUG progress output despite paying formatting
cost. Candidate removes only these three exact progress blocks under the existing asset-progress
suppression control. It retains original cleanup/reflection, all warnings/errors, byte counters,
converter/layout/GL policy, handler construction and caches. Exact loader SHA-256 is
`7d89b44c9401a122529450d17407dbfc8d52e13a9f7eb941dc93125eb5fc153b`.

The same log contains 21,059 per-rule progress messages. The exact Windows Rules class
(`72f0925d83ff48bfa2c4b8d2f691b10935d4567dc6ab1e12392a2ee388539df9`) builds that message
from an already loaded rule ID; JSON lookups occur outside the block and remain intact. Compose
its removal through the existing rules-loader pipeline under the same progress control.

Installed tests pass for opcode verification, original cleanup/GL calls, counter writes, exception
boundaries, full-message opt-out, format drift, rule JSON lookups and rule construction. The common
JAR is the exact private installed fixture; no game assets are committed.

Trial criterion: seek all ordinary interactive-menu observations below 18 s across two groups of
three launches, native graphics and 1024x720, with 20-second cooldowns. Report the full range even
if the criterion fails. Six observations would be local repeatability evidence, not a guarantee
for other installations or cold system starts. Keep the stock resource worker, main-thread GL,
1024 ceiling, coherent fallback and established late-resource behavior.

Phase: full verification, then ordinary measurements.

Full Java verification passed in 45.480 s with installed common/core JARs and the required Log4j
fixture. The first full attempt lacked that fixture and failed class loading in an existing
admission test; supplying the installed dependency resolved it. No production workaround was added.
