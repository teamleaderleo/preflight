# Windows repeatable sub-17 work

Objective: beat 16.69 s and obtain repeated ordinary sub-17 interactive menus with correct resources.
Owner: current Codex task. Phase: native candidates measured; retain bounded copy/log reductions and retire failed experiments.
Finish: retain only justified production improvements, preserve the alignment repair, integrate main,
restore the Windows task and retire disposable builds. A single fast observation is insufficient.

Baseline main 9afb23cf, executable 2a6d2282, JAR 3aebbe8dc19a60c1221be69cb2943cce2d8c4a8a45ef18806865db56ddf1f6ef.
Diagnostic cohort 20260906-054326 enabled phase, texture CPU and prepared-load attribution. Its
19.269 s interactive menu is diagnostic only, not an ordinary acceptance sample.

Installed Windows converter bytecode acquires one BufferedImage.getData raster and reads it through
Raster.getPixel; it does not mutate or publish that raster. The 44 packed converter images represent
464,154,156 cumulative bytes in a recent ordinary run, followed by a stock snapshot copy. Candidate:
mark only independently owned private packed converter images, and let the exact Windows converter
borrow their standard raster. All ordinary BufferedImage.getData snapshot behavior remains intact;
unknown/exposed original carriers retain their existing path. Converter loops, color/alpha behavior,
layout, dimensions, sampler/mipmap/reload policy, GL calls and buffer ownership are unchanged.
Bounded counters record actual raster acquisitions and cumulative snapshot bytes avoided.

Raster-only executable `ef53382194ceae72fbb20ba9ea3920554ac60391`, JAR
`a53090440aba81595f8673b6f6f482830da0ded32ac4687c1bb54039496997d3`, passed full verify in 48.808 s
and three-platform CI `33994117295`. Ordinary native cohort `20260906-054950` completed 18.101 /
19.698 / 30.068 s menus (17.007 / 17.927 / 27.317 s graphics). Every run borrowed 44 rasters and
avoided 464,154,156 cumulative copy bytes, balanced 168 alignment changes/restores, committed 15,002
resources, consumed 102 late resources and left zero active/pending buffers. This is a demonstrated
allocation reduction, not an established startup timing win. The slow outlier is retained.

The phase diagnostic records 8.014 s in TEXTURE calls, approximately 5.671 s current-thread CPU
(coarse Windows CPU clock; 228 per-call skew observations), 3.548 s SpecStore and 2.759 s mod
callbacks. Prepared pack reads sum to 9.215 s across callers and overlap other startup work; do not
add them to phase wall time or treat them as 9.215 s of serial critical-path time.

Next composition restores the previously withdrawn exact cleanup/progress formatting removal from
dd6aa61a, now above the repaired RGB alignment path. It removes only reviewed texture DEBUG/INFO
success formatting and per-rule progress messages, under the existing opt-out. Cleanup, warnings,
errors, JSON reads and rule construction remain. Current installed Rules bytecode again confirms
only an already-read rule ID enters the removed StringBuilder block. This is a new combined trial,
not retroactive acceptance of the old native-stall run.

Combined raster reuse + quiet progress executable `1e06d416db82a178117bb9ebb462e37af5e60556`, JAR
`949558859f05da4ff10b883c8c4a6d5cc6d19566216c24bdecbc5cd28c7bbbcc`, passed full verify in 44.567 s
with installed common/core/Log4j fixtures. Cohort `20260906-055827` completed 19.713 / 18.610 /
18.590 s interactive menus, with graphics 17.958 / 16.928 / 16.904 s. Still not sub-17.

Next candidate specializes the private borrowed raster's int getPixel implementation for its exact
RGB/ARGB backing array, eliminating the generic per-band mask/offset loop. The original converter
still makes the same getPixel calls and controls transparency, padding, colors and output layout.
Public BufferedImage snapshots remain standard and independent; the private read-only Raster retains
its standard sample model/data buffer for inherited operations. Tests compare pixel values, supplied
and null output arrays, trailing output entries, invalid coordinates and short arrays, plus installed
RGB/RGBA converter bytes and reload behavior. This differs from the rejected byte-buffer-backed
custom raster: it reads one already-packed int per pixel and has no byte-buffer access or layout
classification in the pixel loop. Performance remains a hypothesis until native measurement.

Specialized raster executable `af712a30b449b220f7fbf21a067fcdde1272485e`, JAR
`eac21129aa9d4d729f628d15f0114f8521734dc0cc7008b9d4ba5a466010075a`, passed full verify in 47.459 s
and three-platform CI `33994940057`. Cohort `20260906-060709` completed 18.144 / 19.687 / 18.688 s
menus (16.332 / 18.041 / 17.133 s graphics). No useful gain over the standard-raster composition
was established. Withdraw the custom getPixel implementation and retain the simpler standard raster.

Temporary host scheduling trial kept 14 vCPUs and the same host CPUs 0–13, but allowed each vCPU to
run anywhere in that pool rather than one-to-one pinning. No low-power cores were added and no
persistent VM configuration changed. Same specialized artifact cohort `20260906-061156` completed
17.680 / 18.531 / 30.602 s menus; graphics 15.929 / 16.847 / 22.601 s. This did not establish better
repeatability. Original live one-to-one affinity was restored and verified (`lc a324fd0dee43e047`).
The outlier also had 5.035 s between process start and the game-log graphics clock origin, versus
0.295 / 0.092 s in the other two runs. Its ready-to-interactive interval was 2.966 s versus
1.456 / 1.592 s. Do not attribute the whole outlier to the texture converter.

Next experiment uses Preflight's existing Fastest (RAW) texture-storage policy. The current manifests
contain 30,638 unique prepared blobs, of which 27,198 are LZ4; total decoded pixels are 5,331,135,254
bytes. Raw blobs are bounded at about 5.335 GB before pack index overhead. Guest volume has about
85 GB free and host volume over 550 GB free, sufficient for a full reversible cache backup and
preparation peak. This changes storage policy, not resource identity or converter/GL behavior.


Fastest preparation was first run with discovery selecting `fr.bat`, then explicitly repeated with
`--launcher C:\Games\Starsector\starsector-core\starsector.bat`. Both resolve the same current
resource profile `cfe95f25f14ce426766539225fd1fdab520d728b117a317413f47d3c40fbae3a`;
explicit-launcher preparation succeeded with lookup verification. `prepare --launcher` already
exists; no CLI change was necessary. The original 2,144-file / 8,091,640,093-byte cache was backed
up before preparation. Reports and rollback receipt live in shared Diagnostics as
`sub17-fastest-native-prepare.json` and `sub17-cache-restore.json`.

RAW native cohort `20260906-064009`, standard-raster/quiet executable `1e06d416` and JAR
`949558859f05da4ff10b883c8c4a6d5cc6d19566216c24bdecbc5cd28c7bbbcc`, completed menus in
20.909 / 21.047 / 20.638 s, graphics 19.659 / 19.208 / 18.936 s. All three had zero LZ4 pack
hits, proving the intended storage mode was used, and zero pack read failures. This is slower
than the compressed composition; restore the backed-up Balanced cache. Do not change the default.

The retained standard-raster/quiet cohort `20260906-055827` and the RAW cohort each report 44 raster
reuses, 464,154,156 bytes of avoided snapshot copies, 168 alignment changes and 168 restores,
and zero active or pending buffers in every run. Combined executable `1e06d416` also passed
three-platform CI `33994545766`. The final production/test tree matches that executable; later
commits record evidence and withdraw the unproven specialized getPixel implementation.

Result: no ordinary interactive run in this task beat 16.69 s; repeatable sub-17 remains unachieved.
Keep the demonstrated allocation/formatting reductions, with no claim of an established startup
speedup. Neither specialized raster reads, shared host CPU affinity, nor RAW storage justified
retention. The compressed composition staged about 14,600 prepared images successfully with only
hundreds of misses, so increasing its 64 MiB queue is not yet justified. Remaining texture work
includes original converter and main-thread GL costs; measured long outliers also extend before
and after graphics loading. Further work should separate those tails before adding more knobs.
