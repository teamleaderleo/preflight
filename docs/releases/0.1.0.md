Preflight prepares reusable caches for an existing Starsector installation and launches the game
with exact, fail-open runtime optimizations. Recommended mode and Balanced texture storage are the
default. The desktop app calculates disk requirements before preparation, keeps profiles separate,
previews cleanup and removal, and can export or send a disclosed diagnostics bundle.

The reviewed development installation's observed record moved from a roughly 101-second worst case
to a 15.88-second fastest warm launch, with an established 88.13-second controlled median at the
start of the measured chronology. Those figures describe one large mod profile on one machine; the
release candidate's fresh before-and-after cohort will be the public comparison.

This beta ships without paid Apple or Windows publisher identities, so those systems may show their
standard warning for an unknown developer. Every download includes a SHA-256 manifest, and desktop
updates also require Preflight's embedded updater signature. Starsector, mods, saves, and game files
remain outside Preflight's update, cleanup, and removal targets.
