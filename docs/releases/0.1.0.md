# Preflight 0.1.0

> **Draft release notes.** Replace the development measurements and package details with the exact
> release-candidate results before tagging or publishing this release.

## Summary

Preflight prepares reusable caches for an existing Starsector installation and launches the game
with exact, fail-open runtime optimizations. Recommended mode and Balanced texture storage are the
defaults. The desktop app calculates disk requirements before preparation, keeps profiles separate,
previews cleanup and removal, and can export or send a disclosed diagnostics bundle.

## Development performance record

The reviewed development installation progressed from a roughly 101-second worst case to a
15.88-second fastest warm launch, with an initial 88.13-second controlled median. Those figures
describe one large mod profile on one machine. The built-in benchmark lets each installation record
its own normal and accelerated launch.

## Package trust

This beta ships without paid Apple or Windows publisher identities, so those systems may show their
standard warning for an unknown developer. Every download includes a SHA-256 manifest, and desktop
updates also require Preflight's embedded updater signature. Starsector, mods, saves, and game files
remain outside Preflight's update, cleanup, and removal targets.
